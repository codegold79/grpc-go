version: "3.7"
services:
  route-server:
    build:
      context: .
      dockerfile: ./server/Dockerfile
    image: fgold/route-server:0.1
  route-client-bdd:
    build:
      context: .
      dockerfile: ./bddTests/Dockerfile.test
    image: fgold/route-client-bdd:0.1
    network_mode: service:route-server
